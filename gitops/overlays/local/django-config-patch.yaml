# Strategic-merge patches: ADD keys to the base ConfigMaps that were
# deliberately left out of base/ (since they differ between local/prod).
# These values point at the local-infra.yaml Services above.
apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
  namespace: app
data:
  DEBUG: "True"
  ALLOWED_HOSTS: "localhost,127.0.0.1,*"
  DATABASE_URL: "postgresql://neuralops:neuralops_password@django-db.data.svc.cluster.local:5432/django_db"
  DATABASE_REPLICA_URL: "postgresql://neuralops:neuralops_password@django-db.data.svc.cluster.local:5432/django_db"
  REDIS_URL: "redis://redis.data.svc.cluster.local:6379/0"
  KAFKA_BOOTSTRAP_SERVERS: "kafka.data.svc.cluster.local:9092"
  ELASTICSEARCH_URL: "http://elasticsearch.data.svc.cluster.local:9200"
  DB_HOST: "django-db.data.svc.cluster.local"
  DB_PORT: "5432"
  DB_USER: "neuralops"
  DB_PASSWORD: "neuralops_password"
  DB_NAME: "django_db"
  REDIS_HOST: "redis.data.svc.cluster.local"
  REDIS_PORT: "6379"
  KAFKA_HOST: "kafka.data.svc.cluster.local"
  EMAIL_BACKEND: "django.core.mail.backends.smtp.EmailBackend"
  EMAIL_HOST: "email-smtp.ap-south-1.amazonaws.com"
  EMAIL_PORT: "587"
  EMAIL_USE_TLS: "True"
  DEFAULT_FROM_EMAIL: "adinathmkclt@gmail.com"
  AWS_S3_ENDPOINT_URL: "http://minio.data.svc.cluster.local:9000"
  AWS_S3_PUBLIC_ENDPOINT_URL: "http://localhost:8080/artifacts"
  AWS_S3_SIGNED_URL_EXPIRY: "900"
