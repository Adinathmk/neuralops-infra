apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: django-secrets
  namespace: app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secret-store
    kind: ClusterSecretStore
  target:
    name: django-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: neuralops/django
