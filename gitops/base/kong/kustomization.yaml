apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
configMapGenerator:
  - name: kong-declarative-config
    namespace: app
    files:
      - kong.yml
generatorOptions:
  disableNameSuffixHash: true # keep name stable ("kong-declarative-config"), not hashed
